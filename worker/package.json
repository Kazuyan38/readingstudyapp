{
  "name": "readingstudyapp-sync-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute DB --file=./schema.sql --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4",
    "typescript": "~5.9",
    "wrangler": "^4"
  }
}
